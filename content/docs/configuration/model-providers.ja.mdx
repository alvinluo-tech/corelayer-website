---
title: モデルプロバイダー
description: Jarvisルーティング用のAIモデルプロバイダーを設定します。
---

CoreLayerのモデルゲートウェイは、複数のAIプロバイダーにリクエストをルーティングします。1つ以上のプロバイダーを設定し、Jarvisが各タスクに最適なモデルを選択できるようにします。

## サポートされているプロバイダー

モデルゲートウェイは、組み込みのプリセットを介して以下のプロバイダーをネイティブにサポートしています:

| プロバイダー | ID | タイプ | 代表的なモデル |
|------------|----|------|----------------|
| **MiMo (小米)** | `mimo` | `openai_compatible` | `mimo-v2.5-pro`, `mimo-2.5` |
| **Groq** | `groq` | `openai_compatible` | `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` |
| **DeepSeek** | `deepseek` | `openai_compatible` | `deepseek-chat`, `deepseek-reasoner` |
| **Anthropic** | `anthropic` | `openai_compatible` | `claude-sonnet-4`, `claude-haiku-4-5` |
| **Google Gemini** | `google` | `openai_compatible` | `gemini-2.5-pro`, `gemini-2.5-flash` |
| **OpenAI** | `openai` | `openai_compatible` | `gpt-4o`, `gpt-4o-mini`, `o3-mini` |
| **通義千問 (Qwen)** | `qwen` | `openai_compatible` | `qwen-max`, `qwen-plus` |
| **OpenRouter** | `openrouter` | `openai_compatible` | `anthropic/claude-sonnet-4`, `google/gemini-2.5-pro` |
| **Ollama** | `ollama` | `ollama` | `llama3.2`, `qwen2.5`, `deepseek-r1` |

## 設定ファイル

CoreLayerは設定と認証情報を分離するセキュリティ設計を採用しています。設定ファイルは `~/.jarvis/config/` ディレクトリに保存されます。

### 1. メイン設定 (`config.json`)

有効なプロバイダー、モデルのルーティングルール、および各プロバイダーのエンドポイントを定義します。**このファイルにはAPIキーは含まれません**:

```json
{
  "version": 1,
  "activeProvider": "mimo",
  "activeModel": "mimo-v2.5-pro",
  "providers": [
    {
      "id": "mimo",
      "name": "MiMo",
      "type": "openai_compatible",
      "baseURL": "https://token-plan-ams.xiaomimimo.com/v1",
      "enabled": true
    },
    {
      "id": "groq",
      "name": "Groq",
      "type": "openai_compatible",
      "baseURL": "https://api.groq.com/openai/v1",
      "enabled": true
    },
    {
      "id": "openrouter",
      "name": "OpenRouter",
      "type": "openai_compatible",
      "baseURL": "https://openrouter.ai/api/v1",
      "enabled": true
    },
    {
      "id": "ollama",
      "name": "Ollama",
      "type": "ollama",
      "baseURL": "http://localhost:11434/v1",
      "enabled": true
    }
  ]
}
```

### 2. 認証情報設定 (`credentials.json`)

APIキーを個別に安全に保存し、`config.json` 内の対応するプロバイダー `id` にマッピングします:

```json
{
  "mimo": "your_mimo_api_key_here",
  "groq": "your_groq_api_key_here",
  "openrouter": "your_openrouter_api_key_here"
}
```

## モデルルーティング

複数のプロバイダーが設定されている場合、モデルゲートウェイは以下に基づいて選択します:

- **タスクの複雑さ** — 複雑な推論はより高性能なモデルにルーティング
- **レイテンシー要件** — 時間に敏感なリクエストはGroqまたはローカルモデルを優先
- **コスト最適化** — 日常タスクはより安価なプロバイダーを使用
- **可用性** — プロバイダーがダウンした場合のフォールバック

## 環境変数

APIキーをハードコードする代わりに環境変数を使用できます:

```bash
GROQ_API_KEY=your_groq_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
```

設定で参照:

```json
{
  "apiKey": "${GROQ_API_KEY}"
}
```

## 次のステップ

- [モデルゲートウェイ](/docs/core-concepts/model-gateway) — ルーティングロジックの理解
- [ローカルモデル設定](/docs/guides/configure-local-models) — Ollamaのセットアップ
